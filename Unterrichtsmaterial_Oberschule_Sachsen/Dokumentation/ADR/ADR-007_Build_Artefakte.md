# ADR-007 – Build-Artefakte

## Status
Akzeptiert

Quellen liegen auf `main`.

Generierte Artefakte werden ausschließlich durch die Build-Pipeline erzeugt und über GitHub Pages veröffentlicht.

Sie werden nicht versioniert.
