# Ausgabe

Dieser Ordner enthält automatisch generierte Dokumente.

Die Quelldateien befinden sich unter:

`Unterrichtsmaterial_Oberschule_Sachsen/`

## Wichtig

Dateien in diesem Ordner werden nicht manuell bearbeitet.

Änderungen erfolgen ausschließlich in den Markdown-Quelldateien und werden anschließend neu gebaut.
